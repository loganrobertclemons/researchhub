All of these are optimized for kali linux. The bash program is untested in ubuntu. It will run, but ubuntu does not
come preloaded with all the packages used and I ran out of time to run through and add all the get commands to the
file. Fake TCP testing creates a broken, fake TCP server. The rest is self explanatory. 